---
name: commit-helper
description: Writes clear git commit messages in our team format. Use whenever the user is committing changes, staging files to commit, or asks for a commit message.
---

# Commit message style

Write every commit message in this format:

    type(scope): summary

Allowed types: feat, fix, docs, refactor, test, chore.
- feat = a new feature
- fix = a bug fix
- docs = documentation only
- refactor = code change that isn't a feature or fix
- test = adding or fixing tests
- chore = tooling, config, dependencies

Rules for the summary:
